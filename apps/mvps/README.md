# Standalone MVP Apps

Each MVP should have a standalone app directory here before it is merged into a milestone.

Naming convention:

```txt
m<major>.<minor>-<short-slug>/
```

Example:

```txt
m1.1-product-shell/
m1.2-auth-workspaces/
```

Each MVP app directory should include a README that links back to its roadmap spec.
